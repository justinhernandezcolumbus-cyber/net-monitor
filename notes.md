# Net Monitor - Project Notes
# 1. Import the necessary libraries
# 2. Define our config
# 3. check the OS that this is running on (bonus if this is agnostic to which OS it's running on)
# 3. Identify any dependencies(maybe is redundant if libraries are the dependencies)
